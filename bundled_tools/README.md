Place optional native binaries here before running PyInstaller.

Expected filenames:
- macOS/Linux: ffmpeg, ffprobe, yt-dlp
- Windows: ffmpeg.exe, ffprobe.exe, yt-dlp.exe

The build spec includes any of these files that exist.
